next-version: v3.0.0
tag-prefix: '[vV]?'

# Conventional Commits Style -> https://www.conventionalcommits.org/en/v1.0.0/
major-version-bump-message: "^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\\([\\w\\s-,/\\\\]*\\))?(!:|:.*\\n\\n((.+\\n)+\\n)?BREAKING CHANGE:\\s.+)"
minor-version-bump-message: "^(feat)(\\([\\w\\s-,/\\\\]*\\))?:"
patch-version-bump-message: "^(fix|perf)(\\([\\w\\s-,/\\\\]*\\))?:"

branches:
  develop:
    regex: ^dev$
    mode: ContinuousDelivery
    label: alpha
    increment: None # Values: Major, Minor, Patch or None

  main:
    regex: ^prod$
    mode: ContinuousDelivery
    label: ''
    increment: None

  feature:
    regex: ^(feat|feats?)[\/-](?<BranchName>.+)
    mode: ContinuousDelivery
    label: '{BranchName}'
    increment: Inherit

  release:
    regex: ^(release|releases?)[/-]
    mode: ContinuousDelivery
    label: rc
    increment: None

  hotfix:
    regex: ^hotfix(es)?[/-]
    mode: ContinuousDelivery
    label: hotfix
    increment: None